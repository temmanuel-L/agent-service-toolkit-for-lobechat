# -*- coding: utf-8 -*-
"""graph_rag_salary 提示词与固定文案。"""

from __future__ import annotations

from agents.graph_rag_salary.schema import format_ontology_summary

EMPTY_CONTEXT_REPLY = (
    "根据当前薪酬知识图谱中的检索结果，无法找到与您问题直接相关的记录。"
    "本库主要包含 Michael Page 等薪酬报告中的行业、岗位、薪资区间、技能与市场趋势；"
    "请勿使用外部公开资料臆测数字。可尝试换一种行业/岗位表述再问。"
)

ANSWER_PROMPT = """你是薪酬报告分析助手。请根据「检索上下文」回答用户问题。
要求：
1. 优先使用上下文中的结构化薪资数字与岗位/行业名称
2. 若某对比组 results 为空，或上下文明确缺失某岗位薪资，必须说明未检索到，严禁编造数字
3. 对比类问题用简洁表格或分点呈现；只展示上下文里真实出现的数值
4. 可引用证据原文，但不要大段照抄
5. 若岗位名带括号限定（如科技与服务），在回答中如实说明匹配情况
6. 同一岗位可有多条带 level 的薪资（如全国平均/华东/华北/华南）：
   - 按 level 分列或表格展示，禁止把不同地区合并成一个更宽的 min-max
   - 用户未提地区：有多档则简要列出各地区；仅全国平均或空 level 则只报该档
   - 用户提了地区（见上下文「用户关注地区口径」）：优先采用该 level，其它地区可简要带过
   - 严禁编造上下文没有的地区数字

用户问题：
{query}

检索上下文：
{context}
"""

CHAT_SYSTEM_PROMPT = (
    "你是友好的薪酬知识助手。当前问题与薪酬知识图谱检索无关，请用中文自然对话回答。"
    "不要编造具体薪资数字；若用户其实想查行业/岗位薪资，可提示其换一种更具体的问法。"
)


def build_l1_intent_system_prompt() -> str:
    """一级意图（chat vs rag）系统说明，附带轻量本体摘要。"""
    ontology = format_ontology_summary(max_node_desc_chars=60)
    return f"""你是薪酬 Graph RAG 助手的一级意图分类器。
判断用户问题是否需要查询薪酬知识图谱（rag），还是普通闲聊（chat）。

知识图谱覆盖范围（与下列本体相关则倾向 rag）：
- 行业/二级行业岗位薪资、薪资对比、行业岗位概览、技能需求与市场趋势
- 数据来源为薪酬报告（如 Michael Page 中国大陆薪酬指南）

本体摘要：
{ontology}

规则：
- 涉及行业、岗位、薪资/薪酬/工资、技能、市场趋势、对比等 → rag
- 问候、闲聊、与薪酬图谱无关的通用问题 → chat
"""
